program: -m [[ package_name ]].train
method: bayes
metric:
  name: val_loss
  goal: minimize
parameters:
  learning_rate:
    distribution: log_uniform
    min: 0.0001
    max: 0.01
  batch_size:
    values: [16, 32, 64]
  epochs:
    values: [10, 20, 50]
